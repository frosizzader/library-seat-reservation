# 图书馆座位预约系统项目状态追踪文档

## 项目概述

图书馆座位预约系统是一款面向高校图书馆的Web应用原型，旨在解决图书馆座位资源紧张、占座现象严重的问题，实现座位资源的公平合理分配与高效利用。系统支持座位查询、预约、签到、违约记录和管理员配置规则等核心功能，覆盖用户登录、数据增删改查、条件查询、状态流转、基础统计和权限控制等软件工程核心能力。

项目采用前后端分离架构，前端基于Vue 3 + Element Plus构建，后端基于Node.js + Express + MySQL实现。系统面向三类用户群体：学生用户（普通读者，使用座位预约功能）、管理员（图书馆工作人员，管理座位和规则）和超级管理员（系统维护人员）。核心价值体现在公平性（先预约先得）、便捷性（浏览器随时访问）、透明性（实时查看座位状态）和可管理性（灵活的规则配置）。

当前项目处于开发启动阶段，已完成需求分析和架构设计，正在进行基础设施搭建工作。项目目标是完成一个可运行的Web应用原型，界面清晰、数据字段完整、操作流程闭环，并能够输出至少3类统计图表或报表。

## 任务完成状态

根据项目开发任务清单，当前任务进度如下：

| 任务编号 | 任务名称 | 状态 | 完成时间 | 备注 |
|---------|---------|---------|------|------|
| T-01 | 项目初始化与环境配置 | COMPLETED | 2024-01-15 | 前后端项目初始化、依赖安装完成 |
| T-02 | 数据库设计与实现 | COMPLETED | 2024-01-15 | MySQL模型定义、关联配置完成 |
| T-03 | 后端基础框架搭建 | COMPLETED | 2024-01-18 | 路由、中间件、JWT认证完成 |
| T-04 | 前端项目搭建 | COMPLETED | 2024-01-18 | 基础架构、路由守卫、全局布局完成 |
| T-05 | 用户认证模块 | IN_PROGRESS | - | 注册登录流已跑通，进行细节完善 |
| T-06 | 座位管理模块 | PENDING | - | - |
| T-07 | 预约模块 | PENDING | - | - |
| T-08 | 签到与状态管理 | PENDING | - | - |
| T-09 | 规则配置模块 | PENDING | - | - |
| T-10 | 统计分析模块 | PENDING | - | - |

## 当前进度

项目整体进度约为 50%，基础设施阶段已完成。

| 阶段 | 任务数 | 已完成 | 进行中 | 待启动 | 完成率 |
|-----|-------|-------|-------|-------|-------|
| 第一阶段：基础设施 | 4 | 4 | 0 | 0 | 100% |
| 第二阶段：核心功能 | 4 | 0 | 1 | 3 | 12.5% |
| 第三阶段：高级功能 | 2 | 0 | 0 | 2 | 0% |
| **合计** | **10** | **4** | **1** | **5** | **45%** |

*注：基础设施任务已全部完成，核心功能开发已启动。*

当前正在进行中的任务：
1. **T-05**: 用户认证模块的细节优化。
2. **T-06**: 座位管理模块的业务逻辑实现。

**项目运行状态**：
- 后端服务：已启动 (https://rn6qg920-3001.jpe1.devtunnels.ms)
- 前端服务：已启动 (http://localhost:5173)
- 数据库：已初始化并包含测试数据

## 下一步行动

根据项目任务依赖关系和优先级排序，接下来应按以下顺序执行开发任务：

### 优先级一：T-01 项目初始化与环境配置

作为所有任务的前置依赖，项目初始化是首要任务。具体工作包括：初始化Node.js后端项目并安装Express、Sequelize、MySQL2、Winston等核心依赖包；初始化Vue 3前端项目并安装Element Plus、Pinia、Vue Router、Axios、ECharts等前端依赖；创建符合技术文档规范的前后端目录结构；配置环境变量文件（.env）和数据库连接配置；验证前后端开发服务器可正常启动。此任务的完成将为后续所有开发任务提供基础工程支撑。

### 优先级二：T-02 数据库设计与实现

在项目初始化完成后，立即进行数据库设计与实现。具体工作包括：创建users用户表（包含username、password、real_name、phone、email、role、status、violation_count等字段，支持student/admin/super_admin三种角色）；创建areas区域表和seats座位表（包含座位编号、区域ID、楼层、状态、设备设施等字段）；创建reservations预约表（包含预约ID、用户ID、座位ID、预约日期、开始时间、结束时间、状态、签到时间等字段）；创建violations违约记录表和rules规则配置表；配置数据库索引和约束确保查询性能；编写并执行测试数据初始化脚本。此任务依赖T-01完成，是后端开发的数据基础。

### 优先级三：T-03 后端基础框架搭建

在数据库设计完成后进行后端基础框架搭建。具体工作包括：配置Express应用入口和中间件链；实现JWT认证中间件，支持Bearer Token验证；实现RBAC权限控制中间件，支持基于角色的接口访问控制；实现统一错误处理中间件和参数校验中间件；搭建RESTful API路由结构（auth、user、seat、reservation、rule、stats六大模块）；封装统一响应格式（code/message/data结构）；配置Sequelize ORM模型关联关系；集成Winston日志系统记录请求和错误日志。此任务依赖T-02完成，为业务逻辑开发提供框架支撑。

### 优先级四：T-04 前端项目搭建

与T-03并行进行的前端基础架构搭建。具体工作包括：配置Vue Router路由系统，设置登录/注册/主页面路由和导航守卫；创建Pinia状态管理stores（user、seat、reservation、app）；封装Axios请求模块，配置请求拦截器和响应拦截器，自动携带JWT Token；全局引入Element Plus组件库并配置主题；实现DefaultLayout布局组件（包含AppHeader、AppSidebar、AppBreadcrumb）；实现登录页面UI和表单验证；配置全局样式和SCSS变量。此任务依赖T-01完成，为前端业务组件开发提供基础架构。

## 遇到的问题

当前项目处于初始阶段，尚未遇到需要记录的技术问题或业务挑战。

在项目推进过程中，可能需要关注以下潜在风险点：数据库连接配置需要确保MySQL服务正常运行；JWT密钥需要妥善保管并设置合理的过期时间；前后端联调时需要统一API接口路径和响应格式；Element Plus组件版本兼容性需要关注；预约时段冲突校验逻辑需要仔细设计；违约规则计算需要考虑边界情况。

如在开发过程中遇到问题，将在本节及时记录并更新解决方案。

## 变更记录

| 日期 | 版本 | 变更内容 | 变更人 |
|-----|---------|-------|
| 2024-01-15 | v0.1 | 初始文档创建，任务清单建立，状态初始化为PENDING | 项目经理 |

## 项目详细任务说明

### 第一阶段：基础设施（P0）

#### T-01 项目初始化与环境配置

**任务类型**：基础设施  
**优先级**：P0  
**依赖关系**：无  
**预估工作量**：中等  
**任务描述**：完成前后端项目初始化、依赖安装、目录结构创建和开发环境配置。

**详细工作内容**：

后端项目初始化需要创建Node.js项目，初始化package.json文件，安装生产依赖包括express（Web应用框架）、sequelize（ORM框架）、mysql2（MySQL驱动）、jsonwebtoken（JWT认证）、bcryptjs（密码加密）、winston（日志记录）、cors（跨域处理）、dotenv（环境变量）、express-validator（参数校验）；安装开发依赖包括nodemon（热重载）、jest（单元测试）。前端项目初始化需要使用Vite创建Vue 3项目，安装依赖包括vue（核心框架）、vue-router（路由管理）、pinia（状态管理）、axios（HTTP客户端）、element-plus（UI组件库）、echarts（图表库）、sass（样式预处理）。目录结构需要按照技术文档规范创建前后端目录结构。

**验收标准**：

- 初始化Node.js后端项目，Express框架依赖安装完成
- 初始化Vue 3前端项目，Element Plus、Vite等依赖安装完成
- 项目目录结构符合技术文档规范
- 配置文件正确创建（.env、数据库配置）
- 前后端开发服务器可正常启动

#### T-02 数据库设计与实现

**任务类型**：数据库  
**优先级**：P0  
**依赖关系**：T-01  
**预估工作量**：中等  
**任务描述**：根据技术文档完成MySQL数据库表结构设计、模型定义和初始化脚本。

**详细工作内容**：

用户表（users）设计需要包含id（主键自增）、username（用户名/学号，唯一）、password（密码加密）、real_name（真实姓名）、phone（手机号）、email（邮箱）、role（角色枚举：student/admin/super_admin）、status（状态枚举：active/banned/inactive）、violation_count（违约次数）、created_at、updated_at等字段。区域表（areas）设计需要包含id、name（区域名称）、floor（楼层）、open_time（开放开始时间）、close_time（开放结束时间）、description（描述）、status（状态）等字段。座位表（seats）设计需要包含id、seat_no（座位编号）、area_id（区域ID外键）、floor（楼层）、status（状态枚举：available/reserved/in_use/maintenance/disabled）、has_power（是否有电源）、has_window（是否靠窗）、description（描述）等字段。预约表（reservations）设计需要包含id、user_id（用户ID外键）、seat_id（座位ID外键）、date（预约日期）、start_time（开始时间）、end_time（结束时间）、status（状态枚举：pending/checked_in/completed/cancelled/violated）、check_in_time（签到时间）、release_time（释放时间）、created_at、updated_at等字段。违约记录表（violations）设计需要包含id、user_id（用户ID外键）、reservation_id（预约ID外键）、type（违约类型）、description（描述）、penalty（处罚）、status（状态）、created_at等字段。规则配置表（rules）设计需要包含id、rule_key（规则键）、rule_value（规则值）、description（描述）、updated_at等字段。

**验收标准**：

- 创建users用户表（支持student/admin/super_admin角色）
- 创建areas区域表和seats座位表
- 创建reservations预约表（合状态流转字段）
- 创建violations违约记录表
- 创建rules规则配置表
- 数据库索引和约束正确设置
- 初始化测试数据脚本可执行

#### T-03 后端基础框架搭建

**任务类型**：后端  
**优先级**：P0  
**依赖关系**：T-02  
**预估工作量**：中等  
**任务描述**：搭建Express后端基础架构，包括路由系统、中间件、错误处理和响应封装。

**详细工作内容**：

应用入口配置需要创建app.js作为应用入口，配置CORS中间件允许跨域请求，配置body-parser解析请求体，配置路由中间件挂载各模块路由。认证中间件实现需要创建auth.js中间件，从请求头提取Authorization Bearer Token，验证Token有效性和过期时间，将用户信息挂载到req.user对象，拦截未授权请求返回401错误。权限中间件实现需要创建rbac.js中间件，定义角色权限映射表，根据用户角色检查接口访问权限，拦截权限不足请求返回403错误。错误处理中间件实现需要创建errorHandler.js中间件，捕获所有未处理错误，统一错误响应格式，记录错误日志到Winston。响应封装需要创建response.js工具模块，封装success和error响应方法，统一响应格式为{code, message, data}。路由结构需要创建六大路由模块：auth路由（登录、注册、获取当前用户）、user路由（个人信息、修改密码）、seat路由（座位查询、详情、管理）、reservation路由（预约创建、列表、详情、签到、取消、释放）、rule路由（规则查询、配置）、stats路由（统计分析）。日志系统需要配置Winston日志记录器，记录请求日志和错误日志，支持日志分级和文件输出。

**验收标准**：

- Express应用入口正确配置
- JWT认证中间件实现
- 统一错误处理中间件实现
- RESTful API路由结构正确
- 统一响应格式封装（code/message/data）
- Sequelize ORM模型关联正确配置
- 日志记录功能正常

#### T-04 前端项目搭建

**任务类型**：前端  
**优先级**：P0  
**依赖关系**：T-01  
**预估工作量**：中等  
**任务描述**：搭建Vue 3前端项目架构，包括路由配置、状态管理、请求封装和布局组件。

**详细工作内容**：

路由配置需要创建router目录和路由配置文件，配置路由守卫进行登录状态检查，配置路由懒加载提升首屏加载速度，定义路由元信息控制页面权限。状态管理需要创建stores目录和Pinia store文件，user store管理用户信息和登录状态，seat store管理座位列表和筛选条件，reservation store管理预约数据和表单状态，app store管理全局状态如加载状态和消息提示。请求封装需要创建utils/request.js封装Axios实例，配置baseURL为后端API地址，配置请求拦截器自动添加Authorization头，配置响应拦截器处理错误和Token过期，导出封装好的请求方法供各模块调用。组件库配置需要在main.js全局引入Element Plus，配置Element Plus图标库，按需引入常用组件，配置Element Plus主题和全局样式。布局组件需要创建layouts目录和布局组件，DefaultLayout包含顶部导航栏、侧边菜单栏、面包屑导航，内容区域渲染路由页面，BlankLayout用于登录注册页面。登录页面需要创建登录表单组件，包含用户名和密码输入框，表单验证规则配置，登录按钮提交处理，错误提示信息展示。

**验收标准**：

- Vue Router路由配置完成，登录/注册/主页面路由正常
- Pinia状态管理stores创建完成
- Axios请求封装，API统一拦截器配置
- Element Plus组件库全局引入
- DefaultLayout布局组件实现
- 登录页面UI实现
- 全局样式和主题配置完成

### 第二阶段：核心功能（P0）

#### T-05 用户认证模块

**任务类型**：全栈  
**优先级**：P0  
**依赖关系**：T-03, T-04  
**预估工作量**：中等  
**任务描述**：实现用户注册、登录、个人信息管理和JWT认证功能。

**详细工作内容**：

后端注册接口需要实现POST /api/v1/auth/register接口，接收username、password、realName、phone参数，使用bcryptjs加密存储密码，数据校验和重复用户名检查，调用userService创建用户记录，返回新用户信息。后端登录接口需要实现POST /api/v1/auth/login接口，接收username、password参数，查询用户验证密码，生成JWT Token设置过期时间，返回Token和用户信息。后端获取用户信息接口需要实现GET /api/v1/auth/me接口，从req.user获取当前用户ID，查询用户完整信息（包括违约次数），返回用户信息。前端登录页面需要实现表单数据绑定和提交处理，调用登录API获取Token，存储Token到localStorage，跳转到首页或之前页面，错误提示和加载状态处理。前端注册页面需要实现表单数据绑定和提交处理，表单验证（用户名格式、密码确认、手机号格式），调用注册API创建账户，注册成功后跳转到登录页面。Token管理需要封装Token存储和获取方法，请求拦截器自动添加Token到请求头，响应拦截器处理Token过期情况，退出登录清除Token和跳转登录页。

**验收标准**：

- POST /api/v1/auth/register注册接口正常，密码加密存储
- POST /api/v1/auth/login登录接口返回JWT token
- GET /api/v1/auth/me获取当前用户信息接口正常
- 前端登录页面表单验证和错误提示正常
- 前端注册页面表单验证和提交功能正常
- Token存储在localStorage，请求自动携带
- 退出登录功能正常

#### T-06 座位管理模块

**任务类型**：全栈  
**优先级**：P0  
**依赖关系**：T-05  
**预估工作量**：中等  
**任务描述**：实现座位列表查询、区域管理、座位状态管理和座位详情功能。

**详细工作内容**：

后端座位列表接口需要实现GET /api/v1/seats接口，支持query参数筛选（areaId、floor、status），支持分页参数（page、pageSize），关联查询区域名称，排序规则优化查询性能，返回座位列表和总数。后端座位详情接口需要实现GET /api/v1/seats/:id接口，查询座位完整信息和区域信息，返回座位详情数据。后端区域列表接口需要实现GET /api/v1/areas接口，查询所有区域及其座位统计，返回区域列表。后端座位管理接口需要实现POST /api/v1/seats（创建）、PUT /api/v1/seats/:id（更新）、DELETE /api/v1/seats/:id（删除）接口，权限控制仅管理员可操作，座位状态变更需要记录操作日志。前端座位列表页面需要实现区域和楼层筛选器，座位状态筛选（空闲/已预约/使用中/维护中/禁用），分页组件和座位卡片列表展示，座位状态颜色区分和图标标识，点击座位查看详情。前端座位地图视图需要实现可视化座位图展示，根据区域和楼层加载座位布局，不同状态座位使用不同颜色标识，点击座位显示详情或跳转预约，点击筛选按钮过滤座位显示。前端座位详情弹窗需要显示座位编号、区域、楼层、设施信息，当前座位状态和可预约时段，预约按钮或查看当前预约信息。

**验收标准**：

- GET /api/v1/seats座位列表接口，支持areaId/floor/status筛选和分页
- GET /api/v1/seats/:id座位详情接口
- GET /api/v1/areas区域列表接口
- POST/PUT /api/v1/seats管理员增改座位接口（权限控制）
- 前端座位列表页面，筛选条件和分页正常
- 前端座位地图可视化展示（状态颜色区分）
- 座位状态：空闲/已预约/使用中/维护中/禁用正确显示

#### T-07 预约模块

**任务类型**：全栈  
**优先级**：P0  
**依赖关系**：T-06  
**预估工作量**：中等  
**任务描述**：实现座位预约功能，包括时段选择、预约创建、预约确认和预约规则校验。

**详细工作内容**：

后端创建预约接口需要实现POST /api/v1/reservations接口，接收seatId、date、startTime、endTime参数，校验预约规则（每日/每周上限），校验座位是否已被预约（时间冲突检测），创建预约记录并返回预约信息，生成取餐码用于签到确认。后端预约列表接口需要实现GET /api/v1/reservations接口，支持筛选参数（status、date），返回当前用户的预约列表，支持分页。后端预约详情接口需要实现GET /api/v1/reservations/:id接口，返回预约详细信息包括座位信息。后端取消预约接口需要实现DELETE /api/v1/reservations/:id接口，校验取消权限和取消时限，更新预约状态为已取消，释放座位资源。前端预约页面需要实现座位选择和日期选择组件，时段选择器组件显示可选时间段，不可选时段置灰处理，预约表单数据校验和提交，预约成功显示确认信息和取餐码。预约冲突校验需要调用API查询座位可预约时段，前端时段选择器动态禁用已预约时段，后端二次校验防止并发预约。预约限制校验需要查询用户当前预约次数，后端校验是否超过每日/每周上限，超限提示用户并阻止提交。

**验收标准**：

- POST /api/v1/reservations创建预约接口，规则校验通过
- GET /api/v1/reservations我的预约列表接口
- GET /api/v1/reservations/:id预约详情接口
- DELETE /api/v1/reservations/:id取消预约接口
- 前端预约页面，时段选择器组件正常
- 预约冲突校验（座位已被预约时间不可选）
- 预约成功显示取餐码/确认信息
- 预约限制校验（每日/每周上限）

#### T-08 签到与状态管理

**任务类型**：全栈  
**优先级**：P0  
**依赖关系**：T-07  
**预估工作量**：中等  
**任务描述**：实现签到确认、座位释放、超时自动取消和违约记录功能。

**详细工作内容**：

后端签到接口需要实现POST /api/v1/reservations/:id/checkin接口，接收取餐码或预约ID参数，校验签到时限（预约后30分钟内），更新预约状态为已签到，更新座位状态为使用中，记录签到时间。后端释放座位接口需要实现PUT /api/v1/reservations/:id/release接口，更新预约状态为已完成，更新座位状态为空闲，记录释放时间，计算实际使用时长。后端超时检测需要实现定时任务检测超时预约，扫描状态为待签到且超时的预约，更新为违约状态并记录违约记录，更新用户违约次数。后端违约记录接口需要实现GET /api/v1/violations接口，返回用户违约记录列表，管理员可查看所有违约记录。前端我的预约页面需要实现当前预约和历史预约Tab切换，预约卡片显示座位、时间、状态信息，签到按钮和释放座位按钮操作，预约取消按钮和取消确认弹窗。前端签到功能需要实现签到按钮点击处理，调用签到API完成签到，签到成功提示并更新预约状态，签到失败显示错误原因。前端违约记录页面需要实现违约记录列表展示，违约详情弹窗显示违约原因和处罚，违约次数和状态显示。

**验收标准**：

- POST /api/v1/reservations/:id/checkin签到接口正常
- PUT /api/v1/reservations/:id/release释放座位接口正常
- 超时未签到自动标记为违约
- 违约记录正确创建和关联用户
- 前端签到按钮和释放座位功能正常
- 前端我的预约页面状态显示正确
- 前端违约记录页面展示正常

### 第三阶段：高级功能（P1）

#### T-09 规则配置模块

**任务类型**：全栈  
**优先级**：P1  
**依赖关系**：T-05  
**预估工作量**：中等  
**任务描述**：实现预约规则配置、违约规则配置、开放时间配置和黑名单管理功能。

**详细工作内容**：

后端规则查询接口需要实现GET /api/v1/rules接口，返回所有规则配置列表，支持按规则键查询单个规则。后端规则更新接口需要实现PUT /api/v1/rules/:key接口，仅管理员可操作，参数校验和更新规则值，记录规则变更日志。预约时段配置需要实现时段定义（每时段时长如2小时）、开放预约时间范围（如提前1天可预约）、每日预约时段数量限制。预约限制配置需要实现每日预约上限（如最多3个）、每周预约上限（如最多10个）、同时预约数量上限。签到时限配置需要实现最晚签到时间（预约后30分钟内）、超时自动违约规则。违约规则配置需要实现违约次数阈值（如3次加入黑名单）、处罚措施（禁止预约N天）、黑名单自动管理。开放时间配置需要实现各区域开放时间设置、工作日和节假日分别配置、临时闭馆配置。黑名单管理需要实现查询黑名单用户列表、手动添加/移除黑名单、黑名单用户限制登录和预约。前端规则配置页面需要实现规则表单展示和编辑、时段配置可视化、限制数量输入框、保存和重置按钮。前端黑名单管理页面需要实现黑名单用户列表、搜索和筛选功能、添加用户到黑名单、移除用户出黑名单。

**验收标准**：

- GET /api/v1/rules规则查询接口正常
- PUT /api/v1/rules/:key规则更新接口正常（管理员权限）
- 预约时段配置生效并影响预约逻辑
- 预约限制配置生效并影响预约创建
- 签到时限配置生效并触发超时违约
- 违约规则配置生效并自动处理黑名单
- 前端规则配置页面功能完整
- 前端黑名单管理页面功能完整

#### T-10 统计分析模块

**任务类型**：全栈  
**优先级**：P1  
**依赖关系**：T-09  
**预估工作量**：中等  
**任务描述**：实现使用率统计、预约趋势分析、违约分析和热力图展示功能，输出至少3类统计图表。

**详细工作内容**：

后端使用率统计接口需要实现GET /api/v1/stats/usage接口，支持按区域、楼层、时间范围筛选，统计总座位数、已使用数、使用率，返回使用率数据列表。后端预约趋势接口需要实现GET /api/v1/stats/trend接口，支持按日/周/月维度统计，统计每日预约量、完成量、取消量、违约量，返回趋势数据列表。后端违约分析接口需要实现GET /api/v1/stats/violation接口，统计总体违约率、违约类型分布、违约用户排行，返回违约分析数据。后端热力图数据接口需要实现GET /api/v1/stats/heatmap接口，统计各座位预约频次，按区域和时间段聚合，返回热力图数据。前端统计仪表盘需要实现概览卡片显示关键指标、今日预约量、当前使用率、违约率、用户总数，图表区域展示主要统计图表。前端使用率图表需要实现ECharts柱状图展示各区域使用率，饼图展示座位状态分布，支持时间范围筛选。前端预约趋势图表需要实现ECharts折线图展示预约量趋势，支持日/周/月切换，支持多指标对比。前端违约分析图表需要实现ECharts饼图展示违约类型分布，柱状图展示违约用户排行，趋势图展示违约率变化。前端热力图展示需要实现ECharts热力图展示座位热门程度，按区域和时间段显示，颜色深浅表示预约频次。前端数据导出功能需要实现导出按钮生成Excel报表，支持选择导出时间范围和指标。

**验收标准**：

- GET /api/v1/stats/usage使用率统计接口正常
- GET /api/v1/stats/trend预约趋势接口正常
- GET /api/v1/stats/violation违约分析接口正常
- GET /api/v1/stats/heatmap热力图数据接口正常
- 前端统计仪表盘展示关键指标
- 使用率统计图表（柱状图/饼图）正常显示
- 预约趋势图表（折线图）正常显示
- 违约分析图表（饼图/柱状图）正常显示
- 热力图展示正常
- 数据导出功能正常

## 项目里程碑规划

| 里程碑 | 包含任务 | 目标完成时间 | 交付物 |
|-------|---------|-------------|-------|
| M1 基础设施完成 | T-01, T-02, T-03, T-04 | 第1-2周 | 可运行的前后端基础项目，数据库初始化完成 |
| M2 MVP版本 | T-05, T-06, T-07, T-08 | 第3-5周 | 完成用户登录、座位查询、预约、签到核心功能 |
| M3 正式版本 | T-09, T-10 | 第6-8周 | 完成规则配置、统计分析全部功能，输出统计报表 |

## 技术架构总览

### 前端技术栈

| 技术 | 版本 | 用途 |
|-----|------|------|
| Vue 3 | 3.x | 核心框架，使用Composition API |
| Element Plus | 2.x | UI组件库 |
| Pinia | 2.x | 状态管理 |
| Vue Router | 4.x | 路由管理 |
| Vite | 5.x | 构建工具 |
| Axios | 1.x | HTTP客户端 |
| ECharts | 5.x | 统计图表 |
| SCSS | - | 样式预处理 |

### 后端技术栈

| 技术 | 版本 | 用途 |
|-----|------|------|
| Node.js | 18+ | 运行时环境 |
| Express | 4.x | Web应用框架 |
| MySQL | 8.0 | 关系型数据库 |
| Sequelize | 6.x | 数据库ORM |
| JWT | - | 身份认证 |
| Winston | - | 日志记录 |
| Nginx | - | 反向代理 |
| PM2 | - | 进程管理 |

### 数据库表结构概览

| 表名 | 说明 | 主要字段 |
|-----|------|---------|
| users | 用户表 | id, username, password, role, status, violation_count |
| areas | 区域表 | id, name, floor, open_time, close_time |
| seats | 座位表 | id, seat_no, area_id, floor, status, has_power, has_window |
| reservations | 预约表 | id, user_id, seat_id, date, start_time, end_time, status |
| violations | 违约记录表 | id, user_id, reservation_id, type, penalty |
| rules | 规则配置表 | id, rule_key, rule_value |
| system_logs | 系统日志表 | id, user_id, action, detail |

## 开发规范与约定

### 命名规范

- 数据库表名使用小写蛇形命名法（如user_reservations）
- 字段名使用小写蛇形命名法（如created_at）
- API路由使用小写连字符（如/api/v1/seat-list）
- JavaScript变量和函数使用小驼峰命名法（如getUserInfo）
- Vue组件名使用大驼峰命名法（如UserProfile.vue）
- 常量使用大写下划线命名法（如MAX_RESERVATION_COUNT）

### API响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
```

错误响应格式：

```json
{
  "code": 400,
  "message": "参数错误",
  "data": null
}
```

### 状态码定义

| 状态码 | 说明 |
|-------|------|
| 200 | 成功 |
| 201 | 创建成功 |
| 400 | 请求参数错误 |
| 401 | 未授权/Token无效 |
| 403 | 权限不足 |
| 404 | 资源不存在 |
| 409 | 资源冲突（如座位已被预约） |
| 500 | 服务器内部错误 |

### Git分支策略

| 分支名 | 用途 |
|-------|------|
| main | 主分支，生产环境代码 |
| develop | 开发分支，集成测试代码 |
| feature/T-xx | 功能分支，特定任务开发 |
| hotfix | 热修复分支，紧急bug修复 |

---

**文档版本**：v0.1  
**最后更新**：2024-01-15  
**下次更新**：任务T-01启动时